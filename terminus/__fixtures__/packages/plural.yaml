automation:
  - id: mb_plural_test
    alias: 'MB: Plural form test'
    mode: single
    triggers:
      - platform: state
        entity_id: binary_sensor.is_dark
        to: 'on'
    conditions:
      - condition: state
        entity_id: person.norman
        state: home
    actions:
      - service: light.turn_on
        target:
          entity_id: light.mb_led_one
