ANTHROPIC_API_KEY=
HASS_SERVER=https://terminus.tanuki-mirzam.ts.net
HASS_TOKEN=
# Absolute path to the main/ worktree (for config CRUD tools)
REPO_ROOT=/Users/zrmn/Documents/home-assistant/main
PORT=3001

# Set terminus_agent_url in the add-on UI to http://<laptop-LAN-IP>:3001
# Use LAN IP (ipconfig getifaddr en0), NOT Tailscale IP — the SSH add-on
# runs in Docker bridge networking and cannot reach Tailscale peers (100.x.x.x).
