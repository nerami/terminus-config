VITE_HA_TOKEN=your_long_lived_access_token
VITE_HA_URL=wss://terminus.tanuki-mirzam.ts.net/api/websocket

# Render mode (dev). "shadow" (default) = <terminus-panel> web component, same as
# Home Assistant. "light" = render into the light DOM for easier debugging.
# VITE_PANEL_MODE=light
