# Secrets
secrets.yaml
*.key
*.pem
*.crt

# HA runtime state
.ha_run.lock
.storage/
.cloud/
.uuid
.HA_VERSION
home-assistant.log*
home-assistant_v2.db*
OZW_Log.txt

# Caches / deps / generated
.cache/
deps/
tts/
.ps4-games*.json

# Community custom integrations (HACS or manual) — on-device, self-updating,
# not tracked here. Add a line per integration; hand-written components stay tracked.
custom_components/hacs/
custom_components/divoom_pixoo/

# Backups / blueprints user data
backups/
image/

# Editor / OS
.DS_Store
.vscode/
*.swp
*~

# Claude / local tooling
.mcp.json
.claude/settings.local.json
.playwright-mcp/
.memsearch/
